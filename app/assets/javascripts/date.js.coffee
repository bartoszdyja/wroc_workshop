$ ->
  $('.date_picker').datepicker(
      startView: 2,
      orientation: "bottom left",
      autoclose: true,
      format: 'yyyy-mm-dd',
      defaultViewDate: { year: 1960}
  )

  $('.datatable').DataTable {}
  
